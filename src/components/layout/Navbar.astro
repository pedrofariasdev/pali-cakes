---
import MobileMenu from "./MobileMenu.astro";
import { getCategories } from "@/lib/categories";

const categories = await getCategories();

const catalogCategories = categories.filter(
  (category) => category.categorySlug !== "packs"
);
---

<header class="navbar">
  <div class="container navbar__container">

    <nav
      class="navbar__menu navbar__menu--left"
      aria-label="Navegação principal esquerda"
    >
      <a href="/sobre">
        Sobre
      </a>

      <div class="navbar__dropdown">
        <button type="button" aria-haspopup="true">
          Catálogo
          <span aria-hidden="true">⌄</span>
        </button>

        <div class="navbar__dropdown-menu">
          {
            catalogCategories.map((category) => (
              <a href={category.href}>
                {category.name}
              </a>
            ))
          }
        </div>
      </div>

      <a href="/packs-festa">
        Packs Festa
      </a>
    </nav>

    <a
      href="/"
      class="navbar__logo"
      aria-label="Ir para a página inicial"
    >
      <img
        src="/images/logos/logo-nav-trimmed.png"
        alt="Pali Cakes"
      />
    </a>

    <nav
      class="navbar__menu navbar__menu--right"
      aria-label="Navegação principal direita"
    >
      <a href="/portfolio">
        Portfólio
      </a>

      <a href="/contactos">
        Contactos
      </a>

      <a href="/faq">
        FAQ
      </a>
      <a
      
        href="/carrinho"
        class="navbar__cart"
        aria-label="Abrir carrinho de encomenda, 0 artigos"
      >
        <span aria-hidden="true">🛒</span>

        <span data-cart-count>
          0
        </span>
      </a>
    </nav>

    <button
      class="navbar__toggle"
      type="button"
      aria-label="Abrir menu"
      aria-expanded="false"
      aria-controls="mobile-menu"
      data-menu-toggle
    >
      <span></span>
      <span></span>
      <span></span>
    </button>

  </div>
</header>

<MobileMenu />

<script>
  import "../../scripts/menu.ts";
</script>
